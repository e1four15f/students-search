<Window x:Class="GUI.Loading"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading" Height="300" Width="300"
        ResizeMode="NoResize">
    <Grid>
        <DockPanel>
            <Image Name="Loader" Margin="20" HorizontalAlignment="Center" VerticalAlignment="Center" Height="200" Width="200" 
                   Source="/GUI;component/resources/img/loading.png" RenderTransformOrigin="0.5,0.5" RenderOptions.BitmapScalingMode="HighQuality"/>
            <!--
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl Name="LoadingInfo" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl> 
            </ScrollViewer>
            -->
        </DockPanel>
    </Grid>
</Window>
